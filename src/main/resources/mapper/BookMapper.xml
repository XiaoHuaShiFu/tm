<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaohuashifu.tm.dao.BookMapper">
  <insert id="insert" parameterType="com.xiaohuashifu.tm.pojo.do0.BookDO">
    insert into book (id, numbering, name, cover_url, create_time)
    values (#{book.id}, #{book.numbering}, #{book.name}, #{book.coverUrl}, now())
  </insert>
  
  <delete id="delete">
  	delete from book where id = #{id};
  </delete>
  
</mapper>